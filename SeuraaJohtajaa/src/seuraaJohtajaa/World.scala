package seuraaJohtajaa

import scala.collection.mutable.Buffer
import java.awt.Graphics2D

object World {
  
  //val ships = Buffer[Ship]()
  
  // Avaruuden piirtäminen on asteroidien piirtämistä
  //def draw(g: Graphics2D) = ships foreach (_.draw(g))
}